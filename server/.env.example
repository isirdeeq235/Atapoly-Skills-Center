# Copy to .env and edit
DATABASE_URL=postgresql://dev:dev@localhost:5432/atapoly
JWT_SECRET=super_secret_change_me
FRONTEND_ORIGIN=http://localhost:3000
PORT=4000

# Admin API key used to protect internal endpoints (set strong value)
ADMIN_API_KEY=change_me_admin_key

# Payment provider keys (optional)
PAYSTACK_SECRET_KEY=
FLUTTERWAVE_SECRET_KEY=

# SMTP (for emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=

# Uploads
UPLOADS_BASE_URL=http://localhost:4000

# AWS S3 (optional - used for production uploads)
AWS_S3_BUCKET=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Optional custom public URL (e.g. CloudFront)
AWS_S3_PUBLIC_URL=

# Supabase service (used by the migration script to copy files from Supabase Storage)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_BUCKET=